 <!-- NAVIGATION -->
<div class="navbar navbar-fixed-top">
    <div class="container">
        <ul class="nav navbar-nav pull-right">
            <li>
                <a href="/home/wip">Home</a>
            </li>
            <li class="dropdown">
                <a class="dropdown-toggle" id="dropdownMenu1" data-toggle="dropdown">
                    Sites
                    <span class="caret"></span>
                </a>
                <ul class="dropdown-menu with-icons with-arrow" role="menu" aria-labelledby="dropdownMenu1" data-bind="foreach:sites">
                    <li role="presentation">
                        <!-- ko if: $index() == 0 -->
                        <div class="arrow-up-nav-dropdown"></div>
                        <!-- /ko -->
                        <a role="menuitem" tabindex="-1" href="#">
                            <span class="fa fa-circle icon"></span>
                            <p>
                                <span data-bind="text:name"></span>
                            </p>
                            <small data-bind="with:address">
                                <span data-bind="text:addressLine1"></span>,
                                <span data-bind="text:city"></span>
                                <span data-bind="text:province"></span>
                                @*<span data-bind="text:postalCode"></span>*@
                            </small>
                        </a>
                    </li>
                </ul>
            </li>
        </ul>
    </div>
</div>

