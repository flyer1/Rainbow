module.exports = function (gulp, plugin, help, utils) {

    help.registerHelp('test', {
        description: 'Runs unit tests on client side code (TODO: tests have to be created!)'
    });

    // Run all of the unit tests 
    gulp.task('test', function () {
        // TODO: We's gots some learning to do!
        return true;
    });

};